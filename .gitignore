.venv/
__pycache__/
*.pyc
.env
reports/
.claude/
# Phase 1 label capture — local training data, may contain financial details. Never commit.
data/
.mypy_cache/
.pytest_cache/
.ruff_cache/
dist/
*.egg-info/
.fixture-state.json

# Real invoices/receipts contain personal & financial data — never commit them.
samples/invoices/*
!samples/invoices/.gitkeep
samples/expected.json

# Bank statement exports (CAMT.053 / MT940) — contain ALL transactions. Never commit.
samples/*.xml
samples/statements/
*.camt053
*.mt940
*.940

